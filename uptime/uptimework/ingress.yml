apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: uptimework-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
    - hosts:
        - ukumawork.xyz.abc
      secretName: homelab-tls
  rules:
    - host: ukumawork.xyz.abc
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: uptimework-service
                port:
                  number: 3001