apiVersion: v1
kind: PersistentVolume
metadata:
  name: uptimework-data-pv
spec:
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: nfs.smb.com
    path: /mnt/ssd/hdd/uptimework
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uptimework-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  volumeName: uptimework-data-pv