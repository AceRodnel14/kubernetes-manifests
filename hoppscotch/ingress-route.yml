apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: hoppscotch-route
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`hoppscotch.xyz.abc`)
      kind: Rule
      services:
        - name: keda-add-ons-http-interceptor-proxy
          namespace: keda
          port: 8080
  tls:
    secretName: homelab-tls