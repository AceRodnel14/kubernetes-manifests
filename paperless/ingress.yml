apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: paperless-web-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
    - hosts:
        - paperless.xyz.abc
      secretName: homelab-tls
  rules:
    - host: paperless.xyz.abc
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: paperless-web-service
                port:
                  number: 8000