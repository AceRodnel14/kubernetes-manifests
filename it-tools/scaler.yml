apiVersion: http.keda.sh/v1alpha1
kind: HTTPScaledObject
metadata:
  name: it-tools-http
  namespace: default
spec:
  hosts:
    - it-tools.xyz.abc
  scaleTargetRef:
    name: it-tools
    service: it-tools-service
    port: 80
  replicas:
    min: 0
    max: 1
  scaledownPeriod: 300