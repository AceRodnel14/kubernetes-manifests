# Persistent Volumes and Claims
apiVersion: v1
kind: PersistentVolume
metadata:
  name: phonepage-config-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: nfs.smb.com
    path: /mnt/ssd/hdd/homepage_cp/config
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: phonepage-config-pvc
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  volumeName: phonepage-config-pv
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: phonepage-icons-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: nfs.smb.com
    path: /mnt/ssd/hdd/homepage_cp/icons
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: phonepage-icons-pvc
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 1Gi
  volumeName: phonepage-icons-pv
---
# Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: phonepage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: phonepage
  template:
    metadata:
      labels:
        app: phonepage
    spec:
      containers:
        - name: phonepage
          image: ghcr.io/gethomepage/homepage:latest
          ports:
            - containerPort: 3000
          env:
            - name: HOMEPAGE_ALLOWED_HOSTS
              value: phonepage.xyz.abc
            - name: LOG_TARGETS
              value: stdout
          volumeMounts:
            - name: config-volume
              mountPath: /app/config
              readOnly: true
            - name: icons-volume
              mountPath: /app/public/icons
              readOnly: true
      volumes:
        - name: config-volume
          persistentVolumeClaim:
            claimName: phonepage-config-pvc
        - name: icons-volume
          persistentVolumeClaim:
            claimName: phonepage-icons-pvc
---
# ClusterIP Service (internal only, Traefik will route)
apiVersion: v1
kind: Service
metadata:
  name: phonepage-service
spec:
  selector:
    app: phonepage
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
#---
## Traefik Middleware: strip `/homepage/` prefix
#apiVersion: traefik.io/v1alpha1
#kind: Middleware
#metadata:
#  name: strip-homepage
#spec:
#  stripPrefix:
#    prefixes:
#      - /homepage
---
# Ingress for Traefik
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: phonepage-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    # traefik.ingress.kubernetes.io/router.middlewares: default-strip-homepage@kubernetescrd
spec:
  rules:
    - host: phonepage.xyz.abc
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: phonepage-service
                port:
                  number: 3000