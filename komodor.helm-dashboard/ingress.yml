apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: komohelm-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
    - hosts:
        - komohelm.xyz.abc
      secretName: homelab-tls
  rules:
    - host: komohelm.xyz.abc
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: helm-dashboard
                port:
                  number: 8080